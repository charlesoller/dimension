{
  "name": "dimension",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:backend": "cd backend && npm install && npm run build && prisma generate && prisma migrate deploy",
    "build:frontend": "cd frontend && npm install && npm run build",
    "start:frontend": "cd frontend && npm run start"
  },

  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
